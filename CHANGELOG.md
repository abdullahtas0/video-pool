## 0.1.0

- Initial release
- Intelligent controller pooling with instance reuse via `swapSource()`
- Visibility-based lifecycle management with intersection ratios
- Thermal throttling and memory pressure handling
- Disk pre-fetching cache (500 MB LRU)
- Audio focus management
- Ready-to-use widgets: `VideoFeedView`, `VideoListView`, `VideoCard`
- iOS and Android native device monitoring
